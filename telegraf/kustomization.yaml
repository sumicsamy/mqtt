resources:
  - deployment.yaml
  - servicemonitor.yaml
  - cert.yaml
  - uwm.yaml


configMapGenerator:
  - name: telegraf-config
    files:
      - telegraf.conf
generatorOptions:
  disableNameSuffixHash: true      